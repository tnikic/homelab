apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: longhorn-komf-pvc
  namespace: books
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: longhorn
  resources:
    requests:
      storage: 200Mi
